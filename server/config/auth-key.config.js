/**
 * Authentication key storage, may change this to be stored in an .ENV file
 * as it will be a bit more secure. Also potentially randomize this (future work). Do not change this.
 *
 * @author Jordan Short
 */
 module.exports = {
  secret: "qu-BQ!'YA(4L^=mpy<L0w3L>ak{i'i"
}